{
  "name": "pixelcipher",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "license": "MIT",
  "dependencies": {
    "prompt-sync": "^4.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^24.6.2",
    "@types/prompt-sync": "^4.2.3"
  },
  "scripts": {
    "build": "tsc --build",
    "serve": "ts-node ./src",
    "start": "yarn run build && node ./dist"
  }
}
